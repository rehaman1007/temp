import { useState } from "react";
import {
  Box,
  Drawer,
  List,
  Typography,
  ListItem,
  ListItemButton,
  ListItemIcon,
  ListItemText,
  Card,
  CardContent,
  Button,
  Chip,
} from "@mui/material";
import { Dashboard, BadgeRounded } from "@mui/icons-material";
import { useNavigate } from "react-router-dom";
import ManageAccountsIcon from "@mui/icons-material/ManageAccounts";
import AssignmentIndIcon from "@mui/icons-material/AssignmentInd";
import { convertToKebabCase } from "../utils/CommonUtilities";
import f1Logo from "../assets/Logos/Fincore_transperent.svg";

const SideBar = () => {
  const user = JSON.parse(sessionStorage.getItem("user"));
  const ROLE_MAP = {
    51: "uco",
    52: "pso",
    53: "frt",
    54: "branch",
    55: "support",
    56: "glif",
    57: "f1bog",
    58: "extusers",
  };
  const menuItems = [
    { text: "Dashboard", icon: <Dashboard />, badge: null },
    { text: "User Management", icon: <ManageAccountsIcon />, badge: null },
    {
      text: "User Request Management",
      icon: <AssignmentIndIcon />,
      badge: null,
    },
    {
      text: "Role Management",
      icon: <BadgeRounded />,
      badge: null,
    },
  ];
  const drawerWidth = 240;
  const navigate = useNavigate();

  const navigateToPage = (item, index) => {
    console.log("hello");
    setSelectedMenuItem(index);
    if (user)
      navigate(`../${ROLE_MAP[user.role]}/${convertToKebabCase(item.text)}`);
  };

  const handleDrawerToggle = () => {};
  const [selectedMenuItem, setSelectedMenuItem] = useState(0);

  const drawer = (
    <Box sx={{ height: "100%", display: "flex", flexDirection: "column" }}>
      <Box sx={{ p: 0, borderBottom: "1px solid #E2E8F0" }}>
        <Box sx={{ display: "flex", alignItems: "center", gap: 2 }}>
          <Box>
            <img
              src={f1Logo}
              style={{ width: 60, height: 60, marginLeft: 3 }}
            />
          </Box>
          <Box>
            <Typography variant="h6" sx={{ fontWeight: 700, lineHeight: 1 }}>
              FinCore
            </Typography>
            <Typography variant="caption" color="text.secondary"></Typography>
          </Box>
        </Box>
      </Box>

      <List sx={{ flexGrow: 1, py: 2 }}>
        {menuItems.map((item, index) => (
          <ListItem key={item.text} disablePadding>
            <ListItemButton
              selected={selectedMenuItem === index}
              onClick={() => navigateToPage(item, index)}
            >
              <ListItemIcon
                sx={{
                  color:
                    selectedMenuItem === index
                      ? "primary.main"
                      : "text.secondary",
                  minWidth: 40,
                }}
              >
                {item.icon}
              </ListItemIcon>
              <ListItemText
                primary={item.text}
                sx={{
                  "& .MuiTypography-root": {
                    fontWeight: selectedMenuItem === index ? 600 : 400,
                    color:
                      selectedMenuItem === index
                        ? "primary.main"
                        : "text.primary",
                  },
                }}
              />
              {item.badge && (
                <Chip
                  size="small"
                  label={item.badge}
                  sx={{
                    height: 20,
                    fontSize: "0.75rem",
                    backgroundColor: "secondary.main",
                    color: "white",
                  }}
                />
              )}
            </ListItemButton>
          </ListItem>
        ))}
      </List>

      <Box sx={{ p: 2, bottom: 0, borderTop: "1px solid #E2E8F0" }}>
        <Card>
          <CardContent sx={{ p: 2 }}>
            <Typography variant="subtitle2" sx={{ fontWeight: 600, mb: 1 }}>
              Need Help?
            </Typography>
            <Typography
              variant="body2"
              color="text.secondary"
              sx={{ mb: 2, fontSize: "0.875rem" }}
            >
              Contact our support team
            </Typography>
            <Button size="small" variant="contained" fullWidth>
              Connect to SCORE
            </Button>
          </CardContent>
        </Card>
      </Box>
    </Box>
  );

  return (
    <>
      {/* Sidebar */}
      <Box
        component="nav"
        sx={{ width: { md: drawerWidth }, flexShrink: { md: 0 } }}
      >
        <Drawer
          variant="temporary"
          // open={mobileOpen}
          onClose={handleDrawerToggle}
          ModalProps={{ keepMounted: true }}
          sx={{
            display: { xs: "block", md: "none" },
            "& .MuiDrawer-paper": {
              boxSizing: "border-box",
              width: drawerWidth,
            },
          }}
        >
          {drawer}
        </Drawer>
        <Drawer
          variant="permanent"
          sx={{
            display: { xs: "none", md: "block" },
            "& .MuiDrawer-paper": {
              boxSizing: "border-box",
              width: drawerWidth,
            },
          }}
          open
        >
          {drawer}
        </Drawer>
      </Box>
    </>
  );
};

export default SideBar;
