insertExecutionLog(userId, clientIp, "UPLOAD_SCRIPT", fileName, "SUCCESS", executed, Duration.between(start, Instant.now()).toMillis(), null);
insertExecutionLog(userId, clientIp, "UPLOAD_SCRIPT", failedSql, "FAILED", executed, Duration.between(start, Instant.now()).toMillis(), e.getMessage());
