import React from "react";
import { Box, Typography } from "@mui/material";

export default function Copyright() {
  const currentYear = new Date().getFullYear();

  return (
    <Box
      component="footer"
      sx={{
        display: "flex",
        // Stack on small screens, row on desktop
        flexDirection: { xs: "column", md: "row" },
        alignItems: "center",
        // This spreads the three child boxes across the footer
        justifyContent: "space-between",
        padding: 2,
        color: "text.secondary",
        borderTop: "1px solid",
        borderColor: "divider",
      }}
    >
      {/* 1. Left Corner: Version Info */}
      <Box sx={{ flex: { md: 1 }, textAlign: "left", mb: { xs: 1, md: 0 } }}>
        <Typography variant="body2">
          <strong>Version</strong> 1.0.0
        </Typography>
      </Box>

      {/* 2. Center: Copyright Info */}
      <Box sx={{ flex: { md: "0 1 auto" }, textAlign: "center" }}>
        <Typography variant="body2">
          {`Copyright © ${currentYear} `}
          <Typography
            component="span"
            variant="body2"
            fontWeight="700"
            color="text.primary"
          >
            State Bank of India
          </Typography>
          {". All rights reserved."}
        </Typography>
      </Box>

      {/* 3. Right Corner: Hidden Spacer (ensures center stays center) */}
      <Box
        sx={{
          flex: { md: 1 },
          textAlign: "right",
          display: { xs: "none", md: "block" },
        }}
      >
        <Typography variant="body2">
          <strong>APM ID</strong> APP08159
        </Typography>
      </Box>
    </Box>
  );
}
