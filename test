  const collationColumns = useMemo(() => {
    const cols = [
      {
        field: "balanceDate",
        headerName: "Balance Date",
        flex: 0.8,
        minWidth: 130,
        valueFormatter: (v) => (v ? dayjs(v).format("DD/MM/YYYY") : "-"),
      },
      {
        field: "branchCode",
        headerName: "Branch",
        flex: 1,
        minWidth: 160,
        ...(branchOptions?.length
          ? {
              type: "singleSelect",
              valueOptions: branchOptions,
            }
          : {}),
        renderCell: (params) => (
          <Box sx={{ lineHeight: "1.2", py: 0.5 }}>
            <Typography variant="body2" sx={{ fontWeight: 700, fontSize: "0.8rem" }}>
              {params.row.branchCode}
            </Typography>
            <Typography variant="caption" sx={{ color: "text.secondary", fontSize: "0.7rem" }}>
              {params.row.branchName}
            </Typography>
          </Box>
        ),
      },
      {
        field: "cgl",
        headerName: "CGL",
        flex: 1,
        minWidth: 160,
        renderCell: (params) => (
          <Box sx={{ lineHeight: 1.2, py: 0.5 }}>
            <Typography variant="body2" sx={{ fontWeight: 700, fontSize: "0.8rem" }}>
              {params.row.cgl}
            </Typography>
            <Typography variant="caption" sx={{ color: "text.secondary", fontSize: "0.7rem" }}>
              {params.row.description}
            </Typography>
          </Box>
        ),
      },
      {
        field: "currency",
        headerName: "Currency",
        flex: 1,
        minWidth: 150,
        renderCell: (params) => (
          <Box sx={{ lineHeight: 1.2, py: 0.5 }}>
            <Typography variant="body2" sx={{ fontWeight: 700, fontSize: "0.8rem" }}>
              {params.row.currency}
            </Typography>
            <Typography variant="caption" sx={{ color: "text.secondary", fontSize: "0.7rem" }}>
              {params.row.currencyName}
            </Typography>
          </Box>
        ),
      },
      {
        field: "balance",
        headerName: "Balance",
        flex: 1,
        minWidth: 160,
        align: "right",
        headerAlign: "right",
        renderCell: (params) => (
          <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'flex-end', width: '100%', height: '100%' }}>
            <Typography
              fontWeight={700}
              color={Number(params.value) < 0 ? "error.main" : "success.main"}
            >
              {new Intl.NumberFormat("en-IN", {
                minimumFractionDigits: 2,
              }).format(params.value || 0)}
            </Typography>
          </Box>
        ),
      },
      {
        field: "inrBalance",
        headerName: "INR Balance",
        flex: 1,
        minWidth: 160,
        align: "right",
        headerAlign: "right",
        renderCell: (params) => (
          <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'flex-end', width: '100%', height: '100%' }}>
            <Typography
              fontWeight={700}
              color={Number(params.value) < 0 ? "error.main" : "success.main"}
            >
              {/* UPDATED: Added currency style and INR currency code */}
              {new Intl.NumberFormat("en-IN", {
                style: "currency",
                currency: "INR",
                minimumFractionDigits: 2,
              }).format(params.value || 0)}
            </Typography>
          </Box>
        ),
      },
    ];

    if (selectedCircle === "ALL") {
      cols.splice(1, 0, {
        field: "circleCode",
        headerName: "Circle",
        flex: 1,
        minWidth: 150,
        type: "singleSelect",
        valueOptions: circleOptions,
        renderCell: (params) => (
          <Box sx={{ lineHeight: "1.2", py: 0.5 }}>
            <Typography variant="body2" sx={{ fontWeight: 700, fontSize: "0.8rem" }}>
              {params.row.circleCode}
            </Typography>
            <Typography variant="caption" sx={{ color: "text.secondary", fontSize: "0.7rem" }}>
              {params.row.circleName}
            </Typography>
          </Box>
        ),
      });
    }

    return cols;
  }, [branchOptions, selectedCircle, circleOptions]);

  const consolidatedColumns = useMemo(
    () => [
      {
        field: "cgl",
        headerName: "CGL",
        flex: 1,
        minWidth: 160,
        renderCell: (params) => (
          <Box sx={{ lineHeight: 1.2, py: 0.5 }}>
            <Typography variant="body2" sx={{ fontWeight: 700, fontSize: "0.8rem" }}>
              {params.row.cgl}
            </Typography>
            <Typography variant="caption" sx={{ color: "text.secondary", fontSize: "0.7rem" }}>
              {params.row.description}
            </Typography>
          </Box>
        ),
      },
      {
        field: "currency",
        headerName: "Currency",
        flex: 1,
        minWidth: 150,
        renderCell: (params) => (
          <Box sx={{ lineHeight: 1.2, py: 0.5 }}>
            <Typography variant="body2" sx={{ fontWeight: 700, fontSize: "0.8rem" }}>
              {params.row.currency}
            </Typography>
            <Typography variant="caption" sx={{ color: "text.secondary", fontSize: "0.7rem" }}>
              {params.row.currencyName}
            </Typography>
          </Box>
        ),
      },
      {
        field: "balance",
        headerName: "Balance",
        flex: 1,
        minWidth: 160,
        align: "right",
        headerAlign: "right",
        renderCell: (params) => (
          <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'flex-end', width: '100%', height: '100%' }}>
            <Typography
              fontWeight={700}
              color={Number(params.value) < 0 ? "error.main" : "success.main"}
            >
              {new Intl.NumberFormat("en-IN", {
                minimumFractionDigits: 2,
              }).format(params.value || 0)}
            </Typography>
          </Box>
        ),
      },
      {
        field: "inrBalance",
        headerName: "INR Balance",
        flex: 1,
        minWidth: 160,
        align: "right",
        headerAlign: "right",
        renderCell: (params) => (
          <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'flex-end', width: '100%', height: '100%' }}>
            <Typography
              fontWeight={700}
              color={Number(params.value) < 0 ? "error.main" : "success.main"}
            >
              {/* UPDATED: Added currency style and INR currency code */}
              {new Intl.NumberFormat("en-IN", {
                style: "currency",
                currency: "INR",
                minimumFractionDigits: 2,
              }).format(params.value || 0)}
            </Typography>
          </Box>
        ),
      },
    ],
    []
  );
