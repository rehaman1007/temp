import { ThemeOptions } from "@mui/material/styles";

export const themeOptions: ThemeOptions = {
  palette: {
    mode: "light",
    background: {
      default: "#e8ecf1", // iOS light gray background
      paper: "rgba(255, 255, 255, 0.25)", // transparent frosted paper
    },
    primary: { main: "#007aff" }, // iOS blue
    secondary: { main: "#34c759" }, // iOS green
    error: { main: "#ff3b30" },
    warning: { main: "#ffcc00" },
    info: { main: "#5ac8fa" },
    success: { main: "#34c759" },
    text: {
      primary: "#1c1c1e",
      secondary: "rgba(28,28,30,0.8)",
      disabled: "rgba(28,28,30,0.4)",
    },
  },

  shape: { borderRadius: 20 },

  typography: {
    fontFamily: " -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif",
    h6: { fontWeight: 600 },
    button: { textTransform: "none", fontWeight: 500 },
  },

  components: {
    // Global background gradient like macOS
    MuiCssBaseline: {
      styleOverrides: {
        body: {
          background: `linear-gradient(135deg, #dde5f0 0%, #f3f6fa 100%)`,
          minHeight: "100vh",
          WebkitFontSmoothing: "antialiased",
          MozOsxFontSmoothing: "grayscale",
        },
      },
    },

    // Transparent AppBar
    MuiAppBar: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.4)",
          backdropFilter: "blur(20px) saturate(180%)",
          WebkitBackdropFilter: "blur(20px) saturate(180%)",
          boxShadow: "0 8px 32px rgba(0,0,0,0.1)",
        },
      },
    },

    // Frosted glass Cards & Paper
    MuiPaper: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.25)",
          backdropFilter: "blur(20px) saturate(180%)",
          WebkitBackdropFilter: "blur(20px) saturate(180%)",
          border: "1px solid rgba(255,255,255,0.18)",
          boxShadow: "0 8px 32px 0 rgba(31, 38, 135, 0.1)",
          borderRadius: 20,
          transition: "all 0.3s ease",
          "&:hover": {
            boxShadow: "0 12px 40px rgba(31, 38, 135, 0.2)",
            transform: "translateY(-2px)",
          },
        },
      },
    },

    // Frosted Dialogs like macOS popups
    MuiDialog: {
      styleOverrides: {
        paper: {
          background: "rgba(255,255,255,0.25)",
          backdropFilter: "blur(25px) saturate(200%)",
          WebkitBackdropFilter: "blur(25px) saturate(200%)",
          border: "1px solid rgba(255,255,255,0.18)",
          borderRadius: 20,
          boxShadow: "0 20px 50px rgba(0,0,0,0.2)",
        },
      },
    },

    // Buttons with subtle hover scaling
    MuiButton: {
      styleOverrides: {
        root: {
          borderRadius: 12,
          padding: "6px 16px",
          transition: "all 0.3s ease",
          backdropFilter: "blur(10px)",
          "&:hover": {
            transform: "scale(1.03)",
            backgroundColor: "rgba(255,255,255,0.2)",
          },
        },
      },
    },

    // TextFields with iOS-style borders
    MuiOutlinedInput: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.2)",
          backdropFilter: "blur(10px)",
          borderRadius: 12,
          "&:hover .MuiOutlinedInput-notchedOutline": {
            borderColor: "rgba(0,0,0,0.2)",
          },
          "&.Mui-focused .MuiOutlinedInput-notchedOutline": {
            borderColor: "#007aff",
            borderWidth: 2,
          },
        },
        notchedOutline: {
          borderColor: "rgba(0,0,0,0.1)",
        },
      },
    },

    // Tabs
    MuiTab: {
      styleOverrides: {
        root: {
          textTransform: "none",
          fontWeight: 500,
          "&.Mui-selected": { color: "#007aff" },
        },
      },
    },
    MuiTabs: {
      styleOverrides: {
        indicator: {
          backgroundColor: "#007aff",
          height: 3,
          borderRadius: 3,
        },
      },
    },

    // Table cells with frosted background
    MuiTableCell: {
      styleOverrides: {
        root: {
          borderBottom: "1px solid rgba(255,255,255,0.1)",
          backdropFilter: "blur(12px)",
        },
        head: {
          background: "rgba(255,255,255,0.3)",
          fontWeight: 600,
        },
      },
    },

    // Tooltips like iOS callouts
    MuiTooltip: {
      styleOverrides: {
        tooltip: {
          background: "rgba(0,0,0,0.7)",
          backdropFilter: "blur(8px)",
          fontSize: "0.8rem",
        },
      },
    },

    // Snackbars
    MuiSnackbarContent: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.3)",
          backdropFilter: "blur(12px)",
          color: "#1c1c1e",
          borderRadius: 12,
          border: "1px solid rgba(255,255,255,0.2)",
        },
      },
    },

    // Alerts with transparency
    MuiAlert: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.25)",
          backdropFilter: "blur(12px)",
          border: "1px solid rgba(255,255,255,0.18)",
        },
      },
    },
  },
};