import React, { useState, useCallback } from "react";
import {
  Box,
  Button,
  Paper,
  TextField,
  Typography,
  ToggleButton,
  ToggleButtonGroup,
  Alert,
  CssBaseline,
  ThemeProvider,
  createTheme,
  InputAdornment,
  IconButton,
  Tooltip,
  Snackbar,
} from "@mui/material";
// Icons
import LockOutlinedIcon from "@mui/icons-material/LockOutlined";
import LockOpenOutlinedIcon from "@mui/icons-material/LockOpenOutlined";
import VisibilityIcon from "@mui/icons-material/Visibility";
import VisibilityOffIcon from "@mui/icons-material/VisibilityOff";
import ContentCopyIcon from "@mui/icons-material/ContentCopy";
import DeleteOutlineIcon from "@mui/icons-material/DeleteOutlined";
import TerminalIcon from "@mui/icons-material/Terminal";
import SecurityIcon from "@mui/icons-material/Security";
import ArrowForwardIcon from "@mui/icons-material/ArrowForward";
import CheckCircleOutlineIcon from "@mui/icons-material/CheckCircleOutlined";
import KeyOutlinedIcon from "@mui/icons-material/KeyOutlined";
import ApiOutlinedIcon from "@mui/icons-material/ApiOutlined";
// FinCore Assets
import LogoImg from "../src/assets/Fincore_logo.svg";
import fTitle from "../src/assets/Fincore_title.png";
import backgroundImage from "../src/assets/Fincore_bg.png";
// Crypto Utility
import { encryptPayload, decryptPayload } from "./utils/cryptoUtil";

const LOCAL_STORAGE_COMMAND = 'localStorage.getItem("payloadEncryptionKey")';

/* ============================================================
   FINCORE THEME
============================================================ */
const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#4A47A3",
      light: "#726FC3",
      dark: "#39347F",
      contrastText: "#FFFFFF",
    },
    secondary: {
      main: "#7354C7",
    },
    background: {
      default: "#F4F1FB",
      paper: "rgba(255,255,255,0.88)",
    },
    text: {
      primary: "#35317A",
      secondary: "#65627A",
    },
    divider: "rgba(74,71,163,0.12)",
  },
  typography: {
    fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif',
  },
  shape: {
    borderRadius: 12,
  },
  components: {
    MuiButton: {
      styleOverrides: {
        root: {
          textTransform: "none",
          fontWeight: 700,
          borderRadius: 8,
          boxShadow: "none",
          padding: "12px 20px", // Increased padding for bigger buttons
          fontSize: "0.95rem",
          "&:hover": {
            boxShadow: "0 5px 14px rgba(74,71,163,0.18)",
          },
        },
      },
    },
    MuiTextField: {
      // Removed default 'small' size to enforce larger fields
      styleOverrides: {
        root: {
          "& .MuiOutlinedInput-root": {
            backgroundColor: "rgba(255,255,255,0.78)",
            borderRadius: 8,
            fontSize: "1rem", // Larger input text
            "& fieldset": {
              borderColor: "rgba(74,71,163,0.16)",
            },
            "&:hover fieldset": {
              borderColor: "rgba(74,71,163,0.35)",
            },
            "&.Mui-focused fieldset": {
              borderColor: "#4A47A3",
              borderWidth: 1.5,
            },
          },
          "& .MuiInputLabel-root": {
            color: "#69667B",
            fontSize: "1rem", // Larger label text
          },
          "& .MuiInputLabel-root.Mui-focused": {
            color: "#4A47A3",
          },
        },
      },
    },
    MuiToggleButton: {
      styleOverrides: {
        root: {
          textTransform: "none",
          fontWeight: 700,
          borderColor: "rgba(74,71,163,0.15)",
          color: "#55526A",
          padding: "12px 16px",
          fontSize: "0.95rem",
          "&.Mui-selected": {
            backgroundColor: "rgba(74,71,163,0.10)",
            color: "#403C8C",
            borderColor: "rgba(74,71,163,0.28)",
            "&:hover": {
              backgroundColor: "rgba(74,71,163,0.14)",
            },
          },
        },
      },
    },
  },
});

/* ============================================================
   GLASS CARD
============================================================ */
const glassCard = {
  background:
    "linear-gradient(145deg, rgba(255,255,255,0.95), rgba(255,255,255,0.75))",
  backdropFilter: "blur(14px)",
  WebkitBackdropFilter: "blur(14px)",
  border: "1px solid rgba(255,255,255,0.78)",
  boxShadow: "0 12px 35px rgba(65,48,130,0.10)",
};

/* ============================================================
   STEP ITEM (Memoized for performance)
============================================================ */
const StepItem = React.memo(({ number, children }) => {
  return (
    <Box sx={{ display: "flex", alignItems: "flex-start", gap: 1.5 }}>
      <Box
        sx={{
          flexShrink: 0,
          width: 26,
          height: 26,
          borderRadius: "50%",
          background: "linear-gradient(135deg, #4A47A3, #7354C7)",
          color: "#fff",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          fontSize: "0.85rem",
          fontWeight: 700,
          boxShadow: "0 3px 8px rgba(74,71,163,0.20)",
          mt: 0.2, // fine-tune alignment
        }}
      >
        {number}
      </Box>
      <Typography
        sx={{
          color: "#66637A",
          fontSize: { xs: "0.9rem", sm: "0.95rem", md: "1rem" },
          lineHeight: 1.6,
        }}
      >
        {children}
      </Typography>
    </Box>
  );
});
StepItem.displayName = "StepItem";

/* ============================================================
   APP
============================================================ */
export default function App() {
  const [mode, setMode] = useState("decrypt");
  const [sessionKey, setSessionKey] = useState("");
  const [inputData, setInputData] = useState("");
  const [outputData, setOutputData] = useState("");
  const [showKey, setShowKey] = useState(false);
  const [snackbar, setSnackbar] = useState({
    open: false,
    message: "",
    severity: "success",
  });

  const showNotification = useCallback((message, severity = "error") => {
    setSnackbar({ open: true, message, severity });
  }, []);

  const handleCloseSnackbar = useCallback((event, reason) => {
    if (reason === "clickaway") return;
    setSnackbar((prev) => ({ ...prev, open: false }));
  }, []);

  const handleModeChange = useCallback((event, newMode) => {
    if (!newMode) return;
    setMode(newMode);
    setInputData("");
    setOutputData("");
  }, []);

  const handleClearPayload = useCallback(() => {
    setInputData("");
    setOutputData("");
    showNotification("Payload cleared.", "info");
  }, [showNotification]);

  const handleCopy = useCallback(
    async (textToCopy, successMessage = "Copied to clipboard!") => {
      if (!textToCopy) return;
      try {
        await navigator.clipboard.writeText(textToCopy);
        showNotification(successMessage, "success");
      } catch (error) {
        console.error(error?.message || error);
        showNotification("Unable to copy content.", "error");
      }
    },
    [showNotification]
  );

  const formatOutput = useCallback((data) => {
    if (typeof data === "object" && data !== null) {
      return JSON.stringify(data, null, 2);
    }
    try {
      const parsed = JSON.parse(data);
      return JSON.stringify(parsed, null, 2);
    } catch (error) {
      return String(data);
    }
  }, []);

  const handleProcess = useCallback(async () => {
    setOutputData("");
    if (!sessionKey.trim()) {
      showNotification("Session Key is required.", "warning");
      return;
    }
    if (!inputData.trim()) {
      showNotification("Please provide a payload to process.", "warning");
      return;
    }
    try {
      if (mode === "decrypt") {
        const decryptedRaw = await decryptPayload(
          inputData.trim(),
          sessionKey.trim()
        );
        setOutputData(formatOutput(decryptedRaw));
        showNotification("Payload decrypted successfully!", "success");
      } else {
        let parsedInput;
        try {
          parsedInput = JSON.parse(inputData);
        } catch (error) {
          showNotification(
            "Invalid JSON format. Please provide valid JSON before encrypting.",
            "error"
          );
          return;
        }
        const encryptedString = await encryptPayload(
          parsedInput,
          sessionKey.trim()
        );
        setOutputData(encryptedString);
        showNotification("Payload encrypted successfully!", "success");
      }
    } catch (error) {
      console.error(error);
      showNotification(
        error?.message ||
          `Failed to ${mode} payload. Please verify your key and payload.`,
        "error"
      );
    }
  }, [mode, sessionKey, inputData, formatOutput, showNotification]);

  return (
    <ThemeProvider theme={theme}>
      <CssBaseline />

      {/* Main Container - Fully responsive, strictly avoids overflow clipping */}
      <Box
        sx={{
          minHeight: "100vh", // Allows scrolling vertically if needed
          width: "100%", // Avoids horizontal scrollbars
          overflowX: "hidden", 
          display: "flex",
          flexDirection: "column",
          backgroundImage: `url(${backgroundImage})`,
          backgroundSize: "cover",
          backgroundPosition: "center",
          backgroundAttachment: "fixed", // Keeps background stable on mobile scroll
          boxSizing: "border-box",
          p: { xs: 2, sm: 3, md: 4, lg: 5 },
        }}
      >
        <Box
          sx={{
            maxWidth: 1600,
            width: "100%",
            mx: "auto",
            display: "flex",
            flexDirection: "column",
            flex: 1,
          }}
        >
          {/* ====================================================
              TOP HEADER (Badge Only)
          ===================================================== */}
          <Box
            sx={{
              display: "flex",
              justifyContent: "flex-end", // Aligns the badge strictly to the top right
              width: "100%",
              mb: { xs: 2, md: 4 },
            }}
          >
            <Box
              sx={{
                display: "inline-flex",
                alignItems: "center",
                gap: 1,
                px: { xs: 1.5, sm: 2 },
                py: { xs: 0.8, sm: 1 },
                borderRadius: 2.5,
                background: "rgba(255,255,255,0.85)",
                border: "1px solid rgba(74,71,163,0.15)",
                boxShadow: "0 4px 15px rgba(74,71,163,0.08)",
              }}
            >
              <SecurityIcon
                sx={{
                  fontSize: { xs: 16, sm: 18, md: 20 },
                  color: "primary.main",
                }}
              />
              <Typography
                sx={{
                  color: "#4A478C",
                  fontSize: { xs: "0.65rem", sm: "0.75rem", md: "0.8rem" },
                  fontWeight: 800,
                  letterSpacing: "0.5px",
                }}
              >
                FINCORE UTILITY
              </Typography>
            </Box>
          </Box>

          {/* ====================================================
              MAIN LAYOUT GRID
          ===================================================== */}
          <Box
            sx={{
              display: "grid",
              // Stacks on mobile/tablet. Only splits into columns on large desktops.
              // Utilizing fractions completely prevents overlapping horizontally!
              gridTemplateColumns: {
                xs: "1fr",
                lg: "1.15fr 0.85fr",
              },
              gap: { xs: 4, sm: 5, md: 6, lg: 5 },
              width: "100%",
              flex: 1,
            }}
          >
            {/* ==================================================
                LEFT SIDE (Info)
            =================================================== */}
            <Box sx={{ display: "flex", flexDirection: "column" }}>
              {/* Logo & Title Box */}
              <Box
                sx={{
                  display: "flex",
                  alignItems: "center",
                  gap: { xs: 2, sm: 3 },
                  mb: { xs: 3, sm: 4, md: 5 },
                }}
              >
                <Box
                  component="img"
                  src={LogoImg}
                  alt="Logo Icon"
                  sx={{
                    width: { xs: 65, sm: 90, md: 120, lg: 150 }, // Scales gracefully per device
                    height: "auto",
                    objectFit: "contain",
                  }}
                />
                <Box>
                  <Box
                    component="img"
                    src={fTitle}
                    alt="FinCore"
                    sx={{
                      height: { xs: 22, sm: 28, md: 35 },
                      width: "auto",
                      display: "block",
                      mb: 0.5,
                    }}
                  />
                  <Typography
                    sx={{
                      fontSize: { xs: "0.85rem", sm: "0.95rem", lg: "1.1rem" },
                      color: theme.palette.text.secondary,
                      fontWeight: 500,
                    }}
                  >
                    Insight Driven General Ledger
                  </Typography>
                </Box>
              </Box>

              {/* Descriptions & Title */}
              <Box sx={{ mb: { xs: 3, sm: 4 } }}>
                <Typography
                  sx={{
                    color: "#3F3B82",
                    fontWeight: 800,
                    fontSize: {
                      xs: "1.8rem",
                      sm: "2.2rem",
                      md: "2.6rem",
                      lg: "2.8rem",
                    },
                    lineHeight: 1.15,
                    letterSpacing: "-0.5px",
                  }}
                >
                  FinCore Crypto Utility
                </Typography>
                <Typography
                  sx={{
                    color: "#5A5670",
                    fontSize: { xs: "1.05rem", sm: "1.15rem", md: "1.25rem" },
                    fontWeight: 600,
                    mt: 1.5,
                  }}
                >
                  Secure Encryptor & Decryptor
                </Typography>
                <Typography
                  sx={{
                    color: "#6D697C",
                    fontSize: { xs: "0.95rem", sm: "1rem", md: "1.1rem" },
                    lineHeight: 1.6,
                    maxWidth: 700,
                    mt: 2,
                  }}
                >
                  A developer utility for securely decrypting FinCore API
                  responses and encrypting request payloads using the active
                  application session key.
                </Typography>
              </Box>

              {/* =================================================
                  SESSION KEY GUIDE
              ================================================= */}
              <Paper
                elevation={0}
                sx={{
                  ...glassCard,
                  borderRadius: { xs: 3, md: 4 },
                  p: { xs: 2.5, sm: 3, md: 4 },
                  maxWidth: 800,
                }}
              >
                <Box
                  sx={{
                    display: "flex",
                    alignItems: "center",
                    gap: 2,
                    mb: 3,
                  }}
                >
                  <Box
                    sx={{
                      width: 44,
                      height: 44,
                      borderRadius: 2,
                      background: "rgba(74,71,163,0.12)",
                      display: "flex",
                      alignItems: "center",
                      justifyContent: "center",
                    }}
                  >
                    <KeyOutlinedIcon
                      sx={{ fontSize: 24, color: "primary.main" }}
                    />
                  </Box>
                  <Box>
                    <Typography
                      sx={{
                        fontWeight: 800,
                        color: "#403C82",
                        fontSize: { xs: "1rem", sm: "1.1rem" },
                      }}
                    >
                      Obtain Session Key
                    </Typography>
                    <Typography
                      sx={{
                        color: "#777387",
                        fontSize: { xs: "0.85rem", sm: "0.95rem" },
                      }}
                    >
                      Required for payload processing
                    </Typography>
                  </Box>
                </Box>
                <Box
                  sx={{
                    display: "flex",
                    flexDirection: "column",
                    gap: { xs: 1.5, md: 2 },
                  }}
                >
                  <StepItem number="1">
                    Open <strong>FinCore</strong> application in another tab.
                  </StepItem>
                  <StepItem number="2">
                    Open Developer Tools using <strong>F12</strong> or{" "}
                    <strong>Inspect</strong>.
                  </StepItem>
                  <StepItem number="3">
                    Navigate to the <strong>Console</strong> tab.
                  </StepItem>
                  <StepItem number="4">
                    Execute the command below and copy the returned session key.
                  </StepItem>
                </Box>

                {/* COMMAND TERMINAL */}
                <Box sx={{ mt: 3 }}>
                  <Paper
                    elevation={0}
                    sx={{
                      background: "rgba(43,39,76,0.96)",
                      borderRadius: 2,
                      overflow: "hidden",
                      border: "1px solid rgba(255,255,255,0.08)",
                      boxShadow: "0 7px 20px rgba(50,42,100,0.15)",
                    }}
                  >
                    <Box
                      sx={{
                        height: 40,
                        display: "flex",
                        alignItems: "center",
                        px: 2,
                        background: "rgba(0,0,0,0.14)",
                        borderBottom: "1px solid rgba(255,255,255,0.08)",
                      }}
                    >
                      <TerminalIcon
                        sx={{ fontSize: 16, color: "#C5BDF2", mr: 1 }}
                      />
                      <Typography
                        sx={{
                          color: "#D7D2F1",
                          fontSize: "0.75rem",
                          fontWeight: 700,
                          letterSpacing: "0.5px",
                        }}
                      >
                        JAVASCRIPT CONSOLE
                      </Typography>
                    </Box>
                    <Box
                      sx={{
                        display: "flex",
                        alignItems: "center",
                        gap: 1.5,
                        p: { xs: 1.5, sm: 2 },
                      }}
                    >
                      <Typography
                        component="code"
                        sx={{
                          flex: 1,
                          color: "#D6D0FF",
                          fontFamily: '"Fira Code", "Roboto Mono", monospace',
                          fontSize: { xs: "0.85rem", sm: "0.95rem" },
                          lineHeight: 1.5,
                          whiteSpace: "pre-wrap", // Prevents harsh breaks, lets text wrap normally
                          wordBreak: "break-all", // Ensures no overflowing string blocks
                        }}
                      >
                        {LOCAL_STORAGE_COMMAND}
                      </Typography>
                      <Tooltip title="Copy command" arrow>
                        <IconButton
                          onClick={() =>
                            handleCopy(LOCAL_STORAGE_COMMAND, "Command copied!")
                          }
                          sx={{
                            flexShrink: 0,
                            color: "#E8E5FF",
                            background: "rgba(255,255,255,0.1)",
                            "&:hover": { background: "rgba(255,255,255,0.2)" },
                          }}
                        >
                          <ContentCopyIcon sx={{ fontSize: 18 }} />
                        </IconButton>
                      </Tooltip>
                    </Box>
                  </Paper>
                </Box>
              </Paper>
            </Box>

            {/* ==================================================
                RIGHT SIDE (Tool Component)
            =================================================== */}
            <Paper
              elevation={0}
              sx={{
                ...glassCard,
                borderRadius: { xs: 3, md: 4 },
                p: { xs: 2.5, sm: 3, md: 4 },
                display: "flex",
                flexDirection: "column",
                height: "100%", // stretches properly when on desktop grid side-by-side
              }}
            >
              {/* UTILITY HEADER */}
              <Box
                sx={{
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "space-between",
                  mb: 3,
                }}
              >
                <Box sx={{ display: "flex", alignItems: "center", gap: 1.5 }}>
                  <Box
                    sx={{
                      width: 44,
                      height: 44,
                      borderRadius: 2,
                      background:
                        "linear-gradient(135deg, rgba(74,71,163,0.14), rgba(115,84,199,0.12))",
                      display: "flex",
                      alignItems: "center",
                      justifyContent: "center",
                    }}
                  >
                    <ApiOutlinedIcon
                      sx={{ color: "primary.main", fontSize: 24 }}
                    />
                  </Box>
                  <Box>
                    <Typography
                      sx={{
                        color: "#403C82",
                        fontWeight: 800,
                        fontSize: { xs: "1.1rem", sm: "1.2rem" },
                      }}
                    >
                      Payload Processor
                    </Typography>
                    <Typography
                      sx={{
                        color: "#777387",
                        fontSize: { xs: "0.8rem", sm: "0.9rem" },
                      }}
                    >
                      FinCore secure payload utility
                    </Typography>
                  </Box>
                </Box>

                <Box
                  sx={{
                    display: "flex",
                    alignItems: "center",
                    gap: 0.5,
                    px: 1.5,
                    py: 0.6,
                    borderRadius: 5,
                    background: "rgba(46,125,50,0.1)",
                    border: "1px solid rgba(46,125,50,0.2)",
                  }}
                >
                  <CheckCircleOutlineIcon
                    sx={{ fontSize: 16, color: "#3D8B40" }}
                  />
                  <Typography
                    sx={{ color: "#3D7C40", fontSize: "0.75rem", fontWeight: 800 }}
                  >
                    READY
                  </Typography>
                </Box>
              </Box>

              {/* MODE TOGGLE */}
              <ToggleButtonGroup
                color="primary"
                value={mode}
                exclusive
                onChange={handleModeChange}
                fullWidth
                sx={{ mb: 3 }}
              >
                <ToggleButton value="decrypt">
                  <LockOpenOutlinedIcon sx={{ fontSize: 20, mr: 1 }} />
                  Decrypt Response
                </ToggleButton>
                <ToggleButton value="encrypt">
                  <LockOutlinedIcon sx={{ fontSize: 20, mr: 1 }} />
                  Encrypt Request
                </ToggleButton>
              </ToggleButtonGroup>

              {/* PROCESSOR CONTENT */}
              <Box
                sx={{
                  display: "flex",
                  flexDirection: "column",
                  gap: 3,
                  flex: 1, // pushes terminal to the bottom
                }}
              >
                {/* SESSION KEY */}
                <TextField
                  fullWidth
                  label="Session Key"
                  placeholder="payloadEncryptionKey"
                  type={showKey ? "text" : "password"}
                  value={sessionKey}
                  onChange={(e) => setSessionKey(e.target.value)}
                  autoComplete="off"
                  slotProps={{
                    input: {
                      endAdornment: (
                        <InputAdornment position="end">
                          <Tooltip title="Hold to view key" arrow>
                            <IconButton
                              onPointerDown={() => setShowKey(true)}
                              onPointerUp={() => setShowKey(false)}
                              onPointerLeave={() => setShowKey(false)}
                            >
                              {showKey ? (
                                <VisibilityIcon />
                              ) : (
                                <VisibilityOffIcon />
                              )}
                            </IconButton>
                          </Tooltip>
                        </InputAdornment>
                      ),
                    },
                  }}
                />

                {/* INPUT */}
                <TextField
                  fullWidth
                  multiline
                  minRows={5}
                  maxRows={10}
                  label={
                    mode === "decrypt"
                      ? "Encrypted Response"
                      : "JSON Request Payload"
                  }
                  value={inputData}
                  onChange={(e) => setInputData(e.target.value)}
                  placeholder={
                    mode === "decrypt"
                      ? "Paste encrypted response here..."
                      : '{\n  "key": "value"\n}'
                  }
                  autoComplete="off"
                  spellCheck={false}
                  sx={{
                    "& .MuiInputBase-root": {
                      fontFamily: '"Fira Code", "Roboto Mono", monospace',
                      fontSize: { xs: "0.9rem", sm: "1rem" },
                    },
                  }}
                />

                {/* ACTIONS */}
                <Box
                  sx={{
                    display: "flex",
                    gap: 2,
                    flexWrap: "wrap",
                  }}
                >
                  <Button
                    variant="outlined"
                    color="inherit"
                    onClick={handleClearPayload}
                    startIcon={<DeleteOutlineIcon />}
                    sx={{
                      flex: { xs: "1 1 100%", sm: 1 },
                      color: "#5D596D",
                      borderColor: "rgba(74,71,163,0.25)",
                      "&:hover": {
                        borderColor: "rgba(74,71,163,0.45)",
                        background: "rgba(74,71,163,0.06)",
                      },
                    }}
                  >
                    Clear
                  </Button>
                  <Button
                    variant="contained"
                    onClick={handleProcess}
                    endIcon={<ArrowForwardIcon />}
                    sx={{
                      flex: { xs: "1 1 100%", sm: 2 },
                      background: "linear-gradient(90deg, #4A47A3, #5D56B8)",
                      "&:hover": {
                        background: "linear-gradient(90deg, #39347F, #4F49A5)",
                      },
                    }}
                  >
                    {mode === "decrypt" ? "Decrypt Payload" : "Encrypt Payload"}
                  </Button>
                </Box>

                {/* OUTPUT TERMINAL */}
                <Box
                  sx={{
                    flex: 1, // Fills remaining vertical space
                    display: "flex",
                    flexDirection: "column",
                    borderRadius: 2,
                    overflow: "hidden",
                    background: "rgba(40,36,67,0.97)",
                    border: "1px solid rgba(74,71,163,0.20)",
                    boxShadow: "0 8px 22px rgba(45,36,86,0.12)",
                    minHeight: { xs: 220, sm: 280, md: 350 }, // Solid minHeight stops squishing on mobile devices
                    mt: 1,
                  }}
                >
                  <Box
                    sx={{
                      height: 42,
                      display: "flex",
                      alignItems: "center",
                      justifyContent: "space-between",
                      px: 2,
                      background: "rgba(25,22,43,0.5)",
                      borderBottom: "1px solid rgba(255,255,255,0.08)",
                    }}
                  >
                    <Box sx={{ display: "flex", alignItems: "center" }}>
                      <Box sx={{ display: "flex", gap: 0.8, mr: 2 }}>
                        <Box
                          sx={{
                            width: 10,
                            height: 10,
                            borderRadius: "50%",
                            background: "#E58A8A",
                          }}
                        />
                        <Box
                          sx={{
                            width: 10,
                            height: 10,
                            borderRadius: "50%",
                            background: "#D9B96E",
                          }}
                        />
                        <Box
                          sx={{
                            width: 10,
                            height: 10,
                            borderRadius: "50%",
                            background: "#7CBF83",
                          }}
                        />
                      </Box>
                      <TerminalIcon
                        sx={{ fontSize: 18, color: "#C9C3E6", mr: 1 }}
                      />
                      <Typography
                        sx={{
                          color: "#D6D1EB",
                          fontSize: "0.8rem",
                          fontWeight: 700,
                          letterSpacing: "0.5px",
                        }}
                      >
                        OUTPUT RESULT
                      </Typography>
                    </Box>
                    <Tooltip title="Copy result" arrow>
                      <span>
                        <IconButton
                          disabled={!outputData}
                          onClick={() =>
                            handleCopy(
                              outputData,
                              "Result copied to clipboard!"
                            )
                          }
                          sx={{
                            color: "#C8C2DF",
                            "&:hover": { color: "#FFFFFF" },
                            "&.Mui-disabled": { color: "#5A566B" },
                          }}
                        >
                          <ContentCopyIcon sx={{ fontSize: 18 }} />
                        </IconButton>
                      </span>
                    </Tooltip>
                  </Box>
                  <Box
                    sx={{
                      flex: 1,
                      overflowY: "auto",
                      overflowX: "auto",
                      p: 2,
                      "&::-webkit-scrollbar": { width: 8, height: 8 },
                      "&::-webkit-scrollbar-track": {
                        background: "rgba(255,255,255,0.03)",
                      },
                      "&::-webkit-scrollbar-thumb": {
                        background: "rgba(196,188,231,0.35)",
                        borderRadius: 10,
                      },
                      "&::-webkit-scrollbar-thumb:hover": {
                        background: "rgba(196,188,231,0.55)",
                      },
                    }}
                  >
                    {outputData ? (
                      <pre
                        style={{
                          margin: 0,
                          color: "#BDE5C5",
                          fontFamily: '"Fira Code", "Roboto Mono", monospace',
                          fontSize: "0.9rem",
                          lineHeight: 1.6,
                          whiteSpace: "pre-wrap",
                          wordBreak: "break-all",
                        }}
                      >
                        {outputData}
                      </pre>
                    ) : (
                      <Box
                        sx={{
                          height: "100%",
                          display: "flex",
                          alignItems: "center",
                          justifyContent: "center",
                          flexDirection: "column",
                          gap: 1.5,
                          opacity: 0.6,
                        }}
                      >
                        <TerminalIcon
                          sx={{ fontSize: 40, color: "rgba(205,198,235,0.4)" }}
                        />
                        <Typography
                          sx={{
                            color: "rgba(215,209,235,0.7)",
                            fontSize: "0.9rem",
                            fontStyle: "italic",
                          }}
                        >
                          Output will appear here
                        </Typography>
                      </Box>
                    )}
                  </Box>
                </Box>
              </Box>
            </Paper>
          </Box>
        </Box>

        {/* ====================================================
            FOOTER
        ===================================================== */}
        <Box
          sx={{
            py: 3,
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            mt: "auto", // Ensures footer sticks to the absolute bottom of the scroll area
          }}
        >
          <Typography
            sx={{
              color: "rgba(72,68,105,0.65)",
              fontSize: "0.85rem",
              fontWeight: 500,
            }}
          >
            FinCore Application Utility
          </Typography>
        </Box>
      </Box>

      {/* ========================================================
          SNACKBAR
      ========================================================= */}
      <Snackbar
        open={snackbar.open}
        autoHideDuration={4000}
        onClose={handleCloseSnackbar}
        anchorOrigin={{ vertical: "bottom", horizontal: "center" }}
      >
        <Alert
          onClose={handleCloseSnackbar}
          severity={snackbar.severity}
          variant="filled"
          sx={{
            width: "100%",
            borderRadius: 2,
            boxShadow: "0 8px 25px rgba(40,35,80,0.20)",
          }}
        >
          {snackbar.message}
        </Alert>
      </Snackbar>
    </ThemeProvider>
  );
}
