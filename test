import React from "react";
import { Box, Typography } from "@mui/material";

export default function Copyright() {
  const currentYear = new Date().getFullYear();

  return (
    <Box
      component="footer"
      sx={{
        display: "flex",
        flexDirection: { xs: "column", md: "row" },
        alignItems: "center",
        justifyContent: "space-between",
        // Align horizontal padding with the rest of the application
        px: { xs: 2.5, sm: 4, md: 6 }, 
        py: { xs: 1.5, md: 2 },
        color: "text.secondary",
        borderTop: "1px solid",
        borderColor: "rgba(0,0,0,0.08)", // Slightly softer divider line
        width: "100%",
        boxSizing: "border-box",
      }}
    >
      {/* 1. Left Corner (Desktop) / Bottom Row (Mobile) */}
      <Box
        sx={{
          flex: 1,
          display: "flex",
          justifyContent: { xs: "center", md: "flex-start" },
          gap: { xs: 3, md: 0 },
          // Moves this block below the copyright on mobile, keeps it on the left for desktop
          order: { xs: 2, md: 1 }, 
          width: "100%",
        }}
      >
        <Typography variant="body2" sx={{ fontSize: { xs: "0.75rem", md: "0.875rem" } }}>
          <strong>Version</strong> 1.0.0
        </Typography>
        
        {/* Shows APM ID next to Version ONLY on mobile to save vertical space */}
        <Typography variant="body2" sx={{ display: { xs: "block", md: "none" }, fontSize: "0.75rem" }}>
          <strong>APM ID</strong> APP08159
        </Typography>
      </Box>

      {/* 2. Center: Copyright Info */}
      <Box 
        sx={{ 
          flex: "0 1 auto", 
          textAlign: "center", 
          // Pushes Copyright to the top on mobile, keeps it in center on desktop
          order: { xs: 1, md: 2 }, 
          mb: { xs: 0.5, md: 0 } 
        }}
      >
        <Typography variant="body2" sx={{ fontSize: { xs: "0.75rem", md: "0.875rem" } }}>
          {`Copyright © ${currentYear} `}
          <Typography
            component="span"
            variant="inherit"
            fontWeight="700"
            color="text.primary"
          >
            State Bank of India
          </Typography>
          {". All rights reserved."}
        </Typography>
      </Box>

      {/* 3. Right Corner: APM ID (Hidden on Mobile, shown on Desktop) */}
      <Box
        sx={{
          flex: 1,
          textAlign: "right",
          display: { xs: "none", md: "block" },
          order: 3,
        }}
      >
        <Typography variant="body2" sx={{ fontSize: "0.875rem" }}>
          <strong>APM ID</strong> APP08159
        </Typography>
      </Box>
    </Box>
  );
}
