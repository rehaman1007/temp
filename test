useEffect(() => {
  if (!screenConfig) {
    navigate("/404");
    return;
  }

  // Reset UI state
  setRows([]);
  setRemarks("");
  setConfirmDialog({ open: false, type: null, rowId: null });

  fetchData();
}, [selectedMenuItem?.id]);