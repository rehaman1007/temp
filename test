import { ThemeOptions } from '@mui/material/styles';

export const themeOptions: ThemeOptions = {
  ...yourExistingPaletteAndTypography, // keep all original palette, typography, shape etc.
  components: {
    // ---------------- GLOBAL GLASS BASE ----------------
    MuiCssBaseline: {
      styleOverrides: {
        body: {
          background: 'linear-gradient(135deg, #021128 0%, #0e1e40 100%)',
          color: '#FAF3D9',
          backdropFilter: 'blur(12px)',
          WebkitBackdropFilter: 'blur(12px)',
        },
        '*:focus': {
          outline: '3px solid #D4AF37',
          outlineOffset: '2px',
        },
        '*:focus:not(:focus-visible)': {
          outline: 'none',
        },
      },
    },

    // ---------------- APPBAR ----------------
    MuiAppBar: {
      styleOverrides: {
        root: {
          backgroundColor: 'rgba(14, 30, 64, 0.8)',
          backdropFilter: 'blur(10px)',
          borderBottom: '1.5px solid rgba(212, 175, 55, 0.3)',
          boxShadow: '0 8px 32px rgba(0, 0, 0, 0.5)',
        },
      },
    },

    // ---------------- PAPER / CARD ----------------
    MuiPaper: {
      styleOverrides: {
        root: {
          background: 'rgba(255, 255, 255, 0.12)',
          backdropFilter: 'blur(16px) saturate(180%)',
          WebkitBackdropFilter: 'blur(16px) saturate(180%)',
          border: '1px solid rgba(255,255,255,0.2)',
          borderRadius: 20,
          boxShadow: '0 8px 32px 0 rgba(0, 0, 0, 0.37)',
          transition: 'transform 0.4s ease, box-shadow 0.4s ease',
          '&:hover': {
            transform: 'translateY(-4px)',
            boxShadow: '0 12px 48px rgba(212, 175, 55, 0.3)',
          },
        },
      },
    },

    // ---------------- BUTTONS ----------------
    MuiButton: {
      styleOverrides: {
        root: {
          borderRadius: 16,
          transition: 'all 0.4s ease',
          backdropFilter: 'blur(10px)',
          '&:hover': {
            transform: 'scale(1.05)',
            boxShadow: '0 8px 24px rgba(212, 175, 55, 0.5)',
          },
        },
      },
    },

    // ---------------- TEXTFIELD ----------------
    MuiTextField: {
      styleOverrides: {
        root: {
          '& .MuiInputBase-root': {
            backgroundColor: 'rgba(255, 255, 255, 0.08)',
            borderRadius: 12,
            transition: 'all 0.3s ease',
            '&:hover': {
              backgroundColor: 'rgba(255, 255, 255, 0.15)',
            },
            '&.Mui-focused': {
              backgroundColor: 'rgba(212, 175, 55, 0.1)',
              boxShadow: '0 0 12px rgba(212, 175, 55, 0.4)',
            },
          },
        },
      },
    },

    // ---------------- DIALOG ----------------
    MuiDialog: {
      styleOverrides: {
        paper: {
          background: 'rgba(255, 255, 255, 0.1)',
          backdropFilter: 'blur(20px)',
          WebkitBackdropFilter: 'blur(20px)',
          borderRadius: 20,
          boxShadow: '0 12px 50px rgba(0, 0, 0, 0.5)',
          transition: 'transform 0.4s ease',
          '&:hover': {
            transform: 'scale(1.02)',
          },
        },
      },
    },

    // ---------------- TABLE ----------------
    MuiTableCell: {
      styleOverrides: {
        root: {
          borderBottom: '1px solid rgba(255,255,255,0.1)',
          color: '#FAF3D9',
        },
        head: {
          backgroundColor: 'rgba(23, 71, 133, 0.4)',
          color: '#D4AF37',
          fontWeight: 'bold',
        },
      },
    },

    // ---------------- TABS ----------------
    MuiTabs: {
      styleOverrides: {
        indicator: {
          backgroundColor: '#D4AF37',
          height: 3,
        },
      },
    },

    MuiTab: {
      styleOverrides: {
        root: {
          color: '#FAF3D9',
          transition: 'all 0.3s ease',
          '&.Mui-selected': {
            color: '#D4AF37',
          },
          '&:hover': {
            color: '#E1C75E',
          },
        },
      },
    },

    // ---------------- ALERT ----------------
    MuiAlert: {
      styleOverrides: {
        root: {
          borderRadius: 16,
          backdropFilter: 'blur(12px)',
          backgroundColor: 'rgba(16, 20, 64, 0.6)',
          border: '1px solid rgba(212, 175, 55, 0.3)',
        },
      },
    },

    // ---------------- TOOLTIP ----------------
    MuiTooltip: {
      styleOverrides: {
        tooltip: {
          backgroundColor: 'rgba(14, 30, 64, 0.85)',
          backdropFilter: 'blur(8px)',
          border: '1px solid rgba(212,175,55,0.2)',
          fontSize: '0.9rem',
          color: '#FAF3D9',
        },
      },
    },

    // ---------------- SNACKBAR ----------------
    MuiSnackbarContent: {
      styleOverrides: {
        root: {
          backgroundColor: 'rgba(0,0,0,0.6)',
          backdropFilter: 'blur(10px)',
          borderRadius: 12,
          color: '#FAF3D9',
        },
      },
    },
  },
};