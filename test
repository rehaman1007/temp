  return (
    <ThemeProvider theme={theme}>
      <CssBaseline />
      
      {/* This forcefully removes Vite's default #root padding and max-width */}
      <GlobalStyles 
        styles={{
          'html, body, #root': {
            margin: 0,
            padding: 0,
            width: '100%',
            height: '100%',
            maxWidth: 'none',
          }
        }} 
      />

      {/* Main Container with Background applied directly */}
      <Box
        sx={{
          minHeight: "100vh",
          width: "100vw",
          display: "flex",
          alignItems: "center",
          py: 4,
          backgroundImage: `url(${backgroundImage})`,
          backgroundSize: "cover",
          backgroundPosition: "center",
          backgroundRepeat: "no-repeat",
          backgroundAttachment: "fixed",
        }}
      >
        <Container maxWidth="lg">
           {/* ... the rest of your Grid and code stays exactly the same ... */}
