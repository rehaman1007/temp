import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import fs from "fs";
// https://vite.dev/config/

const address = {
  tejas: "http://10.0.242.165:",
  anandi: "http://10.0.19.100:",
};
const ip = address.anandi;
export default defineConfig({
  plugins: [react()],
  server: {
    host: true,
    port: 1000, // as per rehman's force suggestion
    https: {
      key: fs.readFileSync("./localhost-key.pem"),
      cert: fs.readFileSync("./localhost.pem"),
    },
    proxy: {
      "/LS": {
        target: ip + "8081",
        changeOrigin: true,
        secure: false,
        rewrite: (path) => path.replace(/^\/LS/, ""),
      },
      "/UM": {
        target: ip + "8085",
        changeOrigin: true,
        secure: false,
        rewrite: (path) => path.replace(/^\/UM/, ""),
      },
      "/CL": {
        target: ip + "8090",
        changeOrigin: true,
        secure: false,
        rewrite: (path) => path.replace(/^\/CL/, ""),
      },
    },
  },
});
