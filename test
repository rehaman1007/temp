// Remove this:
// @NotNull(message="Currency cannot be null")
// private String currencies;

// Add this:
@NotNull(message="Currencies cannot be null")
private List<String> currencies;



/* CURRENCY */
// Replace the old currency block with this:
if (dto.getCurrencies() != null && !dto.getCurrencies().isEmpty() && !dto.getCurrencies().contains("ALL")) {
    predicates.add(root.get("currency").in(dto.getCurrencies()));
}
