import { useEffect } from "react";
import {
  Box,
  Button,
  Typography,
  Paper,
  Container,
  Stack,
} from "@mui/material";
import { styled } from "@mui/system";
import LogoImg from "../assets/Logos/Fincore_transperent.svg";
import fTitle from "../assets/Logos/Fincore_title.png";
import WarningAmberIcon from "@mui/icons-material/WarningAmber";
import { Refresh, Home } from "@mui/icons-material";

const Background = styled(Box)(() => ({
  position: "absolute",
  inset: 0,
  backgroundImage: "url('/src/assets/glass-bg.jpg')",
  backgroundSize: "cover",
  backgroundPosition: "center",
  opacity: 0.15,
  zIndex: 0,
}));

const BlurOverlay = styled(Box)({
  position: "absolute",
  inset: 0,
  backdropFilter: "blur(40px)",
  zIndex: 1,
});

const Card = styled(Paper)(({ theme }) => ({
  position: "relative",
  zIndex: 2,
  maxWidth: 500,
  width: "100%",
  margin: "auto",
  padding: theme.spacing(4),
  borderRadius: theme.spacing(3),
  backdropFilter: "blur(24px)",
  backgroundColor: "rgba(255, 255, 255, 0.05)",
  border: "1px solid rgba(255,255,255,0.1)",
  textAlign: "center",
  [theme.breakpoints.up("sm")]: {
    padding: theme.spacing(6),
  },
}));

const ServerError = () => {
  useEffect(() => {
    console.error("500 Error: Internal server error occurred");
  }, []);

  const handleRefresh = () => {
    window.location.reload();
  };

  return (
    <Box
      sx={{
        minHeight: "100vh",
        position: "relative",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        p: 2, 
        background:
          "linear-gradient(to bottom right, rgba(239, 68, 68, 0.08), rgba(0,0,0,0.4))",
      }}
    >
      <Background />
      <BlurOverlay />

      <Container
        maxWidth="sm"
        sx={{ zIndex: 2, display: "flex", justifyContent: "center" }}
      >
        <Card>
          {/* BRANDING LOGO & TITLE IMAGES */}
          <Box
            sx={{
              mb: 4,
              display: "flex",
              justifyContent: "center",
              alignItems: "center",
              gap: 1.5,
            }}
          >
            <Box
              component="img"
              src={LogoImg}
              alt="FinCore Logo"
              sx={{
                height: 36,
                width: "auto",
                objectFit: "contain"
              }}
            />
            <Box
              component="img"
              src={fTitle}
              alt="FinCore"
              sx={{
                height: 22,
                width: "auto",
                objectFit: "contain"
              }}
            />
          </Box>

          {/* ERROR ICON */}
          <Box sx={{ display: "flex", justifyContent: "center", mb: 3 }}>
            <Box
              sx={{
                p: 2,
                borderRadius: "50%",
                backgroundColor: "rgba(239, 68, 68, 0.15)",
                display: "inline-flex",
              }}
            >
              <WarningAmberIcon sx={{ fontSize: 40, color: "#EF4444" }} />
            </Box>
          </Box>

          {/* ERROR CODE & TITLE */}
          <Typography
            variant="h1"
            sx={{
              fontWeight: 800,
              mb: 1,
              background: "linear-gradient(to right, #EF4444, #F97316)",
              WebkitBackgroundClip: "text",
              WebkitTextFillColor: "transparent",
              fontSize: { xs: "2.5rem", sm: "3.5rem" },
            }}
          >
            500
          </Typography>

          <Typography variant="h5" sx={{ mb: 2, fontWeight: 600 }}>
            Internal Server Error
          </Typography>

          <Typography
            sx={{ color: "text.secondary", mb: 4, fontSize: "0.95rem" }}
          >
            Our systems are experiencing technical difficulties. Our engineering
            team has been automatically notified and is actively working to
            resolve this issue.
          </Typography>

          {/* NOTICE CONTAINER */}
          <Paper
            elevation={0}
            sx={{
              backgroundColor: "rgba(239, 68, 68, 0.04)",
              border: "1px solid rgba(239, 68, 68, 0.15)",
              borderRadius: 2,
              p: 2,
              mb: 4,
            }}
          >
            <Typography
              variant="subtitle2"
              sx={{ color: "#EF4444", fontWeight: 600, mb: 0.5 }}
            >
              Security Guarantee
            </Typography>
            <Typography
              variant="body2"
              sx={{ color: "text.secondary", fontSize: "0.85rem" }}
            >
              Your session remains securely isolated. No unauthorized access has
              occurred. Please try again or check back in a few minutes.
            </Typography>
          </Paper>

          {/* ACTION BUTTONS */}
          <Stack
            spacing={2}
            direction={{ xs: "column", sm: "row" }}
            justifyContent="center"
          >
            <Button
              onClick={handleRefresh}
              variant="outlined"
              startIcon={<Refresh sx={{ fontSize: 18 }} />}
              fullWidth
              sx={{
                borderColor: "rgba(255,255,255,0.15)",
                color: "text.primary",
                "&:hover": {
                  borderColor: "rgba(255,255,255,0.3)",
                  backgroundColor: "rgba(255,255,255,0.05)",
                },
              }}
            >
              Refresh Page
            </Button>
            <Button
              variant="contained"
              startIcon={<Home sx={{ fontSize: 18 }} />}
              href="/"
              fullWidth
            >
              Return to Login
            </Button>
          </Stack>
        </Card>
      </Container>
    </Box>
  );
};

export default ServerError;
