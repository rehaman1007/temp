import { createTheme } from "@mui/material/styles";
import bgImg from "../assets/BackGrounds/bg1.png";

const glassBg = "rgba(255, 255, 255, 0.12)";
const glassBorder = "1px solid rgba(255, 255, 255, 0.25)";
const glassShadow =
  "0 4px 30px rgba(0, 0, 0, 0.1), inset 0 1px 1px rgba(255, 255, 255, 0.1)";

const LiquidGlassTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#5D3FD3",
      light: "#A28DF0",
      dark: "#3E2C70",
      contrastText: "#FFFFFF",
    },
    secondary: {
      main: "#F5C542",
      light: "#FFDD67",
      dark: "#C09D24",
      contrastText: "#1E1E1E",
    },
    success: { main: "#4CAF50" },
    warning: { main: "#FFC107" },
    error: { main: "#E53935" },
    info: { main: "#00BCD4" },
    background: {
      default: "rgba(0,0,0,0)",
      paper: glassBg,
    },
    text: {
      primary: "rgba(255,255,255,0.95)",
      secondary: "rgba(255,255,255,0.7)",
      disabled: "rgba(255,255,255,0.5)",
    },
    divider: "rgba(255,255,255,0.15)",
  },

  typography: {
    fontFamily: "'Poppins', sans-serif",
    button: { textTransform: "none", fontWeight: 600 },
    h6: { fontWeight: 600 },
  },

  shape: { borderRadius: 14 },

  components: {
    // 🌫 Global CSS baseline
    MuiCssBaseline: {
      styleOverrides: {
        body: {
          backgroundImage: `url(${bgImg})`,
          backgroundSize: "cover",
          backgroundPosition: "center",
          backgroundRepeat: "no-repeat",
          backgroundAttachment: "fixed",
          height: "100vh",
          margin: 0,
          padding: 0,
          color: "rgba(255,255,255,0.9)",
          backdropFilter: "blur(18px)",
          WebkitBackdropFilter: "blur(18px)",
        },
        "*::-webkit-scrollbar": { width: 8 },
        "*::-webkit-scrollbar-thumb": {
          backgroundColor: "rgba(255,255,255,0.2)",
          borderRadius: 8,
        },
      },
    },

    // 🧊 Paper (Cards, Dialogs, etc.)
    MuiPaper: {
      styleOverrides: {
        root: {
          background: glassBg,
          backdropFilter: "blur(18px)",
          WebkitBackdropFilter: "blur(18px)",
          border: glassBorder,
          boxShadow: glassShadow,
          borderRadius: 16,
        },
      },
    },

    // 📦 Box (for surfaces)
    MuiBox: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.06)",
          backdropFilter: "blur(12px)",
        },
      },
    },

    // 🪟 Dialog
    MuiDialog: {
      styleOverrides: {
        paper: {
          background: "rgba(255,255,255,0.15)",
          backdropFilter: "blur(24px)",
          border: glassBorder,
          boxShadow: glassShadow,
        },
      },
    },

    // 📋 Drawer
    MuiDrawer: {
      styleOverrides: {
        paper: {
          background: "rgba(40,40,60,0.25)",
          backdropFilter: "blur(18px)",
          borderRight: glassBorder,
          boxShadow: "0 4px 20px rgba(0,0,0,0.2)",
        },
      },
    },

    // 🔘 Buttons
    MuiButton: {
      styleOverrides: {
        root: {
          borderRadius: 14,
          padding: "8px 20px",
          backdropFilter: "blur(12px)",
          transition: "all 0.3s ease",
        },
        containedPrimary: {
          background:
            "linear-gradient(135deg, rgba(93,63,211,1) 0%, rgba(162,141,240,1) 100%)",
          boxShadow: "0 2px 8px rgba(93,63,211,0.4)",
          "&:hover": {
            background:
              "linear-gradient(135deg, rgba(162,141,240,1) 0%, rgba(93,63,211,1) 100%)",
            boxShadow: "0 4px 12px rgba(93,63,211,0.6)",
          },
        },
        containedSecondary: {
          background:
            "linear-gradient(135deg, rgba(245,197,66,1) 0%, rgba(255,221,103,1) 100%)",
          color: "#1E1E1E",
          "&:hover": {
            background:
              "linear-gradient(135deg, rgba(255,221,103,1) 0%, rgba(245,197,66,1) 100%)",
          },
        },
        outlined: {
          borderColor: "rgba(255,255,255,0.3)",
          color: "rgba(255,255,255,0.9)",
          "&:hover": {
            borderColor: "#A28DF0",
            background: "rgba(162,141,240,0.1)",
          },
        },
      },
    },

    // 🧩 Inputs, TextFields, Selects, Autocomplete
    MuiOutlinedInput: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.06)",
          backdropFilter: "blur(12px)",
          "& fieldset": { borderColor: "rgba(255,255,255,0.2)" },
          "&:hover fieldset": { borderColor: "#A28DF0" },
          "&.Mui-focused fieldset": { borderColor: "#5D3FD3" },
        },
        input: { color: "rgba(255,255,255,0.9)" },
      },
    },
    MuiInputLabel: {
      styleOverrides: {
        root: { color: "rgba(255,255,255,0.7)" },
      },
    },
    MuiSelect: {
      styleOverrides: {
        select: {
          color: "rgba(255,255,255,0.9)",
          background: "rgba(255,255,255,0.06)",
        },
      },
    },
    MuiAutocomplete: {
      styleOverrides: {
        paper: {
          background: "rgba(255,255,255,0.15)",
          backdropFilter: "blur(14px)",
          border: glassBorder,
        },
      },
    },

    // 🪙 Chips
    MuiChip: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.15)",
          backdropFilter: "blur(12px)",
          border: glassBorder,
          color: "#fff",
        },
      },
    },

    // 🔔 Snackbar
    MuiSnackbarContent: {
      styleOverrides: {
        root: {
          background: "rgba(40,40,60,0.4)",
          backdropFilter: "blur(12px)",
          color: "#fff",
          borderRadius: 12,
          border: glassBorder,
        },
      },
    },

    // ⚠️ Alerts
    MuiAlert: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.15)",
          backdropFilter: "blur(10px)",
          border: glassBorder,
          color: "#fff",
        },
        standardSuccess: { borderLeft: "4px solid #4CAF50" },
        standardError: { borderLeft: "4px solid #E53935" },
        standardWarning: { borderLeft: "4px solid #FFC107" },
        standardInfo: { borderLeft: "4px solid #00BCD4" },
      },
    },

    // 🧱 Tables & DataGrids
    MuiTable: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.08)",
          backdropFilter: "blur(10px)",
        },
      },
    },
    MuiTableCell: {
      styleOverrides: {
        root: {
          borderBottom: "1px solid rgba(255,255,255,0.1)",
          color: "rgba(255,255,255,0.9)",
        },
        head: {
          fontWeight: 600,
          color: "rgba(255,255,255,0.95)",
        },
      },
    },
    MuiDataGrid: {
      styleOverrides: {
        root: {
          background: "rgba(255,255,255,0.06)",
          backdropFilter: "blur(12px)",
          border: glassBorder,
          borderRadius: 14,
          color: "rgba(255,255,255,0.9)",
        },
        columnHeaders: {
          background: "rgba(255,255,255,0.1)",
          backdropFilter: "blur(10px)",
        },
      },
    },

    // 🧭 Icon Buttons
    MuiIconButton: {
      styleOverrides: {
        root: {
          color: "rgba(255,255,255,0.9)",
          "&:hover": {
            background: "rgba(255,255,255,0.15)",
            backdropFilter: "blur(10px)",
          },
        },
      },
    },

    // 🧮 Grid, Paper, Card consistency
    MuiGrid: {
      styleOverrides: {
        root: { color: "rgba(255,255,255,0.9)" },
      },
    },
  },
});

export default LiquidGlassTheme;