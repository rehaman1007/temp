const filters = filterModel.items
  .filter(
    (f) =>
      f.operator &&
      f.value !== undefined &&
      f.value !== null &&
      f.value !== ""
  )
  .map((f) => ({
    field: f.field,
    operator: f.operator,
    value: f.value,
  }));