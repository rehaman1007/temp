Object value = rs.getObject(i);

columns.append(column);

if (value == null) {

    values.append("NULL");

} else if (value instanceof java.sql.Timestamp ts) {

    values.append(
        "TO_TIMESTAMP('")
            .append(ts.toLocalDateTime())
            .append("','YYYY-MM-DD HH24:MI:SS.FF')"
    );

} else if (value instanceof java.sql.Date dt) {

    values.append(
        "TO_DATE('")
            .append(dt)
            .append("','YYYY-MM-DD')"
    );

} else if (value instanceof Number) {

    values.append(value);

} else if (value instanceof Boolean b) {

    values.append(b ? 1 : 0);

} else {

    values.append("'")
            .append(value.toString().replace("'", "''"))
            .append("'");
}






String columnType = meta.getColumnTypeName(i);





if ("DATE".equalsIgnoreCase(columnType)) {

    values.append(
        "TO_DATE('")
            .append(rs.getDate(i))
            .append("','YYYY-MM-DD')"
    );

} else if (columnType.contains("TIMESTAMP")) {

    Timestamp ts = rs.getTimestamp(i);

    values.append(
        "TO_TIMESTAMP('")
            .append(ts.toLocalDateTime())
            .append("','YYYY-MM-DD HH24:MI:SS.FF')"
    );
}