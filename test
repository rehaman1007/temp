// Custom Toolbar containing the bulk update button
const CustomToolbar = ({ onBulkClick, isBulkDisabled }) => (
  <GridToolbarContainer sx={{ p: 2, justifyContent: "flex-end" }}>
    <StyledButton
      variant="contained"
      color="primary"
      startIcon={<UpdateIcon />}
      onClick={onBulkClick}
      disabled={isBulkDisabled}
    >
      Carry Forward Unchanged Rates
    </StyledButton>
  </GridToolbarContainer>
);


  return (
    <StyledContainerBox>
      <StyledDataGridContainer
        rows={rows}
        columns={columns}
        disableRowSelectionOnClick
        pageSizeOptions={[5, 10, 25, 50]}
        initialState={{
          pagination: { paginationModel: { pageSize: 10 } },
          sorting: { sortModel: [{ field: "updatedAt", sort: "desc" }] },
        }}
        loading={isLoading || isBulkSubmitting}
        slots={{
          noRowsOverlay: CustomNoRowsOverlay,
          toolbar: CustomToolbar,
        }}
        slotProps={{
          toolbar: {
            onBulkClick: () => setIsBulkConfirmDialogOpen(true),
            // We pass the calculated boolean down to the toolbar here:
            isBulkDisabled: isBulkSubmitting || rows.length === 0 || isEditDisabled,
          },
        }}
      />

      {/* --- BULK CONFIRMATION DIALOG --- */}
      {/* ... the rest of your dialogs remain exactly the same ... */}
