import React from "react";

import {
  Box,
  Button,
  Dialog,
  DialogActions,
  DialogContent,
  DialogTitle,
  Stack,
  Typography,
} from "@mui/material";

import { DeleteSweep, WarningAmber } from "@mui/icons-material";

const ConfirmDialog = ({ open, onClose, onConfirm }) => {
  return (
    <Dialog
      open={open}
      onClose={onClose}
      maxWidth="xs"
      fullWidth
      PaperProps={{
        sx: {
          borderRadius: 4,
          overflow: "hidden",
        },
      }}
    >
      {/* ================================================= */}
      {/* HEADER */}
      {/* ================================================= */}

      <DialogTitle
        sx={{
          p: 0,
        }}
      >
        <Box
          sx={{
            px: 3,
            py: 2,

            background: "linear-gradient(135deg,#991b1b,#dc2626)",

            color: "white",
          }}
        >
          <Stack direction="row" spacing={1.5} alignItems="center">
            <WarningAmber />

            <Typography variant="h6" fontWeight={800}>
              Dangerous SQL Detected
            </Typography>
          </Stack>
        </Box>
      </DialogTitle>

      {/* ================================================= */}
      {/* CONTENT */}
      {/* ================================================= */}

      <DialogContent
        sx={{
          pt: "24px !important",
          pb: 2,
        }}
      >
        <Typography
          variant="body1"
          sx={{
            lineHeight: 1.8,
          }}
        >
          This SQL statement can modify or permanently delete production data.
        </Typography>

        <Box
          sx={{
            mt: 2,
            p: 2,
            borderRadius: 3,

            bgcolor: "rgba(239,68,68,0.08)",

            border: "1px solid rgba(239,68,68,0.2)",
          }}
        >
          <Typography variant="body2" fontWeight={700} color="error">
            Please verify the query before continuing.
          </Typography>
        </Box>
      </DialogContent>

      {/* ================================================= */}
      {/* ACTIONS */}
      {/* ================================================= */}

      <DialogActions
        sx={{
          px: 3,
          pb: 3,
        }}
      >
        <Button
          variant="outlined"
          onClick={onClose}
          sx={{
            borderRadius: 3,
            px: 3,
          }}
        >
          Cancel
        </Button>

        <Button
          variant="contained"
          color="error"
          disableElevation
          startIcon={<DeleteSweep />}
          onClick={onConfirm}
          sx={{
            borderRadius: 3,
            px: 3,

            background: "linear-gradient(135deg,#dc2626,#ef4444)",

            boxShadow: "0 8px 24px rgba(239,68,68,0.35)",

            "&:hover": {
              transform: "translateY(-1px)",
            },
          }}
        >
          Execute Anyway
        </Button>
      </DialogActions>
    </Dialog>
  );
};

export default ConfirmDialog;
