int progress =
        totalStatements == 0
                ? 0
                : Math.min(
                        99,
                        (executedCount * 100) / totalStatements
                );