          {/* Role Box */}
          <Box
            sx={{
              p: 1.5,
              mb: 1.5,
              borderRadius: 2,
              backgroundColor: (theme) => `${theme.palette.info.main}14`,
              borderLeft: "5px solid",
              borderColor: "info.main",
              display: "flex",
              alignItems: "center", // You may want to change this to "flex-start" if the text gets very long
              gap: 1.5,
            }}
          >
            <Badge sx={{ fontSize: 22, color: "info.main", flexShrink: 0, mt: 0.5 }} />
            <Box sx={{ minWidth: 0 }}>
              <Typography
                variant="body2"
                sx={{ fontWeight: 700, lineHeight: 1.3 }}
                noWrap
              >
                {user?.roleName || "UNKNOWN"}
              </Typography>
              {user?.roleDescription && (
                <Typography
                  variant="caption"
                  color="text.secondary"
                  // Removed noWrap here
                  sx={{ 
                    lineHeight: 1.3,
                    display: "block",
                    wordBreak: "break-word" 
                  }}
                >
                  {user.roleDescription}
                </Typography>
              )}
            </Box>
          </Box>

          {/* Access Box */}
          <Box
            sx={{
              p: 1.5,
              mb: 1,
              borderRadius: 2,
              backgroundColor: (theme) => `${theme.palette.warning.main}14`,
              borderLeft: "5px solid",
              borderColor: "warning.main",
              display: "flex",
              alignItems: "center", // Consider "flex-start" here too if wrapping spans multiple lines
              gap: 1.5,
            }}
          >
            <Security
              sx={{ fontSize: 22, color: "warning.main", flexShrink: 0, mt: 0.5 }}
            />
            <Box sx={{ minWidth: 0 }}>
              <Typography
                variant="body2"
                sx={{ fontWeight: 700, lineHeight: 1.3 }}
                noWrap
              >
                {user?.accessName || "UNASSIGNED"}
              </Typography>
              {user?.accessDescription && (
                <Typography
                  variant="caption"
                  color="text.secondary"
                  // Removed noWrap here
                  sx={{ 
                    lineHeight: 1.3,
                    display: "block",
                    wordBreak: "break-word"
                  }}
                >
                  {user.accessDescription}
                </Typography>
              )}
            </Box>
          </Box>
