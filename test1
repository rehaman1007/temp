// src/components/AdhocReportsCard.jsx
import React from "react";
import { motion } from "framer-motion";
import { Paper, Box, Typography, IconButton, Tooltip } from "@mui/material";
import {
  DescriptionRounded,
  DownloadRounded,
  ArrowForwardRounded,
} from "@mui/icons-material";
import CustomChip from "../../../utils/CustomChip";

const AdhocReportsCard = ({ report, onClick, onDownload }) => {
  return (
    <motion.div
      whileHover={{
        y: -6,
        transition: { duration: 0.3, type: "spring", stiffness: 300 },
      }}
      style={{ height: "100%" }}
    >
      <Paper
        onClick={onClick}
        elevation={0}
        tabIndex={0}
        onKeyDown={(e) => {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            onClick();
          }
        }}
        sx={{
          p: 3,
          borderRadius: 3,
          height: "100%",
          display: "flex",
          flexDirection: "column",
          cursor: "pointer",
          bgcolor: "background.paper",
          // Soft Shadow Stack
          boxShadow:
            "0 4px 6px -1px rgba(0, 0, 0, 0.02), 0 2px 4px -1px rgba(0, 0, 0, 0.02)",
          transition: "all 0.3s cubic-bezier(0.4, 0, 0.2, 1)",
          "&:hover": {
            boxShadow:
              "0 20px 25px -5px rgba(0, 0, 0, 0.05), 0 10px 10px -5px rgba(0, 0, 0, 0.01)",
            "& .card-icon-bg": {
              transform: "scale(1.1) rotate(-5deg)",
              bgcolor: "primary.light",
              color: "primary.contrastText",
            },
            "& .action-arrow": { transform: "translateX(5px)", opacity: 1 },
          },
          "&:focus-visible": {
            outline: "2px solid",
            outlineColor: "primary.main",
            outlineOffset: 2,
          },
        }}
      >
        <Box
          sx={{
            display: "flex",
            alignItems: "flex-start",
            justifyContent: "space-between",
            mb: 2,
          }}
        >
          <Box
            className="card-icon-bg"
            sx={{
              p: 1.5,
              borderRadius: 2.5,
              bgcolor: "grey.50",
              color: "primary.main",
              display: "flex",
              transition: "all 0.3s ease",
            }}
          >
            <DescriptionRounded fontSize="medium" />
          </Box>
          <Tooltip title="Download" arrow>
            <IconButton
              size="small"
              onClick={(e) => {
                e.stopPropagation();
                onClick();
              }}
              sx={{
                color: "grey.400",
                "&:hover": { color: "primary.main", bgcolor: "primary.50" },
              }}
            >
              <DownloadRounded fontSize="small" />
            </IconButton>
          </Tooltip>
        </Box>

        <Box sx={{ flexGrow: 1, mb: 2 }}>
          <Typography
            variant="h6"
            fontWeight="700"
            color="text.primary"
            gutterBottom
            sx={{ fontSize: "1.05rem" }}
          >
            {report.reportName}
          </Typography>
          <Typography
            variant="body2"
            color="text.secondary"
            sx={{
              display: "-webkit-box",
              WebkitLineClamp: 3,
              WebkitBoxOrient: "vertical",
              overflow: "hidden",
              lineHeight: 1.6,
            }}
          >
            {report.reportDescription || "No description available."}
          </Typography>
        </Box>

        <Box
          sx={{
            display: "flex",
            alignItems: "center",
            justifyContent: "space-between",
            pt: 1.5,
          }}
        >
          <CustomChip
            label={report.category || "Report"}
            size="small"
            sx={{
              height: 24,
              fontSize: "0.7rem",
              fontWeight: 600,
              borderRadius: 1,
              bgcolor: "grey.100",
              color: "text.secondary",
            }}
          />
          <ArrowForwardRounded
            className="action-arrow"
            fontSize="small"
            sx={{
              color: "primary.main",
              opacity: 0,
              transform: "translateX(0)",
              transition: "all 0.3s ease",
            }}
          />
        </Box>
      </Paper>
    </motion.div>
  );
};

export default AdhocReportsCard;
